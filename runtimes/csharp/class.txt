// auto generated by xls2cfg,do not edit!!!
using SimpleJSON;
using System;
using System.Text;
using System.Collections.Generic;

namespace {{namespace}} {
    // {{classComment}}
    public sealed class {{className}} {
        {% for field in fields -%}
        public{% if fieldReadonly %} readonly{% endif %} {{field.typename}} {{field.name}};   // {{field.comment}}
        {% endfor %}

        public {{className}} () {}

        public {{className}} (JSONNode jsonNode) {
            {% for field in fields -%}
            {{formatFieldFromJson(field.index)}}
            {% endfor %}
        }

        public {{className}} (ByteStream bs) {
            {% for field in fields -%}
            {{formatFieldFromBinary(field.index)}}
            {% endfor %}
        }

        public JSONNode ToJson() {
            JSONObject jsonNode = new JSONObject();
            {% for field in fields -%}
            {{formatFieldToJson(field.index)}}
            {% endfor %}
            return jsonNode;
        }

        public void Serialize(ByteStream bs) {
            {% for field in fields -%}
            {{formatFieldToBinary(field.index)}}
            {% endfor %}
        }

        public override string ToString() {
            return this.ToJson().ToString();
        }

        public byte[] SerializeToJson() {
            return Encoding.UTF8.GetBytes(this.ToJson().ToString());
        }

        public byte[] SerializeToBinary() {
            ByteStream bs = ByteStream.GetFromPool();
            this.Serialize(bs);
            byte[] data = bs.ToBytes();
            ByteStream.PutToPool(bs);
            return data;
        }

        public static {{className}} DeserializeFromJson(byte[] data) {
            if (data == null || data.Length == 0) {
                return new {{className}}();
            }
            string str = Encoding.UTF8.GetString(data);
            JSONNode jsonNode = JSON.Parse(str);
            return new {{className}}(jsonNode);
        }

        public static {{className}} DeserializeFromBinary(byte[] data) {
            if (data == null || data.Length == 0) {
                return new {{className}}();
            }
            ByteStream bs = ByteStream.GetFromPool();
            bs.CopyBuffer(data);
            {{className}} obj = new {{className}}(bs);
            ByteStream.PutToPool(bs);
            return obj;
        }

        public static Dictionary<{{idTypename}},{{className}}> DeserializeTableFromJson(byte[] data) {
            if (data == null || data.Length == 0) {
                return new Dictionary<{{idTypename}}, {{className}}>();
            }
            string str = Encoding.UTF8.GetString(data);
            JSONNode jsonNode = JSON.Parse(str);
            Dictionary<{{idTypename}}, {{className}}> table = new Dictionary<{{idTypename}}, {{className}}>(jsonNode.Count);
            {{className}} obj = null;
            foreach(JSONNode entry in jsonNode) {
                obj = new {{className}}(entry); table.Add(obj.{{idName}}, obj);
            }
            return table;
        }

        public static Dictionary<{{idTypename}},{{className}}> DeserializeTableFromBinary(byte[] data) {
            if (data == null || data.Length == 0) {
                return new Dictionary<{{idTypename}}, {{className}}>();
            }
            ByteStream entry = ByteStream.GetFromPool();
            entry.CopyBuffer(data);
            int count = entry.ReadUInt16();
            Dictionary<{{idTypename}}, {{className}}> table = new Dictionary<{{idTypename}}, {{className}}>(count);
            {{className}} obj = null;
            for (int i=0; i<count; i++) {
                obj = new {{className}}(entry); table.Add(obj.{{idName}}, obj);
            }
            ByteStream.PutToPool(entry);
            return table;
        }

        public static byte[] SerializeTableToJson(Dictionary<{{idTypename}},{{className}}> table) {
            JSONArray arr = new JSONArray();
            if (table != null && table.Count > 0) {
                List<{{idTypename}}> keys = new List<{{idTypename}}>(table.Keys);
                keys.Sort();
                for (int i = 0; i < keys.Count; i++) {
                    arr.Add(table[keys[i]].ToJson());
                }
            }
            return Encoding.UTF8.GetBytes(arr.ToString());
        }

        public static byte[] SerializeTableToBinary(Dictionary<{{idTypename}},{{className}}> table) {
            int count = table == null ? 0 : table.Count;
            if (count > 65535) {
                throw new Exception("table row count > 65535");
            }
            ByteStream bs = ByteStream.GetFromPool();
            bs.WriteUInt16((ushort)count);
            if (count > 0) {
                List<{{idTypename}}> keys = new List<{{idTypename}}>(table.Keys);
                keys.Sort();
                for (int i = 0; i < keys.Count; i++) {
                    table[keys[i]].Serialize(bs);
                }
            }
            byte[] data = bs.ToBytes();
            ByteStream.PutToPool(bs);
            return data;
        }
    }
}