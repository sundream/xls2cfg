// auto generated by xls2cfg,do not edit!!!
using System;
using System.Collections.Generic;

namespace {{namespace}} {
    // {{classComment}}
    public sealed class {{className}} {
        {% for field in fields -%}
        public {{field.typename}} {{field.name}};   // {{field.comment}}
        {% endfor %}

        public {{className}}({{baseClassName}} baseRow, {{levelClassName}} levelRow) {
            if (baseRow == null) {
                throw new ArgumentNullException(nameof(baseRow));
            }
            if (levelRow == null) {
                throw new ArgumentNullException(nameof(levelRow));
            }
            {% for field in fields -%}
            {% if field._name != 'id' -%}
            this.{{field.name}} = baseRow.{{field.name}};
            {% endif -%}
            {% endfor %}
            {% for field in levelOverrideFields -%}
            this.{{field.name}} = levelRow.{{field.name}};
            {% endfor %}
            this.{{idName}} = levelRow.{{levelTableIdName}};
            this.{{levelFieldName}} = levelRow.{{levelFieldName}};
        }

        public {{className}}({{baseClassName}} baseRow, int level) {
            if (baseRow == null) {
                throw new ArgumentNullException(nameof(baseRow));
            }
            {% for field in fields -%}
            {% if field._name != 'id' -%}
            this.{{field.name}} = baseRow.{{field.name}};
            {% endif -%}
            {% endfor %}
            this.{{idName}} = ({{idTypename}})baseRow.{{baseIdName}} * {{levelIdFactor}} + level;
            this.{{levelFieldName}} = level;
        }

        public static Dictionary<{{idTypename}}, {{className}}> MergeTable(
            Dictionary<{{baseIdTypename}}, {{baseClassName}}> baseTable,
            Dictionary<{{levelIdTypename}}, {{levelClassName}}> levelTable) {
            var result = new Dictionary<{{idTypename}}, {{className}}>();
            foreach (var basePair in baseTable) {
                var baseRow = basePair.Value;
                {{baseIdTypename}} baseId = baseRow.{{baseIdName}};
                int maxLevel = baseRow.{{maxLevelFieldName}};
                for (int level = 0; level <= maxLevel; level++) {
                    {{idTypename}} rowId = ({{idTypename}})baseId * {{levelIdFactor}} + level;
                    if (levelTable.TryGetValue(rowId, out var levelRow)) {
                        result[rowId] = new {{className}}(baseRow, levelRow);
                    } else {
                        result[rowId] = new {{className}}(baseRow, level);
                    }
                }
            }
            return result;
        }
    }
}
